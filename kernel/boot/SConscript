# Grab the build environment
Import('x86_64_env')
env    = x86_64_env

obj_list = [env.yasm('loader.o', 'loader.asm')]
obj_list += [env.gdc('gdt.o', 'gdt.d')]
obj_list += [env.gdc('kernel.o', 'kernel.d')]

boot = env.PartialLink('boot.o', obj_list)

Return('boot')
