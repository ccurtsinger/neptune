# Grab the build environment
Import('env')

obj_list  = [env.gdc('physical.o', 'physical.d')]
obj_list += [env.gdc('heap.o', 'heap.d')]
obj_list += [env.gdc('virtual.o', 'virtual.d')]

Return('obj_list')
